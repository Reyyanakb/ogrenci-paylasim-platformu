@model mvcFinal2.Models.ListingsViewModel

@{
    ViewData["Title"] = "Tüm İlanlar";
}

<div class="container section-padding">
    
    <!-- Header -->
    <div class="text-center mb-5 fade-in-up">
        <h1 class="fw-bold">İlanları Keşfet</h1>
        <p class="text-muted">Aradığın odayı veya eşyayı kategoriye göre incele.</p>
    </div>

    <!-- Rooms Section -->
    <div class="mb-5 fade-in-up" style="animation-delay: 0.1s;">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2 class="fw-bold"><i class="bi bi-door-open text-warning me-2"></i>Kiralık Oda & Yurt</h2>
            <a asp-action="Rooms" class="btn btn-outline-dark rounded-pill btn-sm px-3">Tümünü Gör</a>
        </div>
        
        @if (Model.Rooms != null && Model.Rooms.Any())
        {
            <div class="row g-4">
                @foreach (var item in Model.Rooms.Take(3)) // Show first 3 here
                {
                    <div class="col-md-4">
                        <div class="card-custom h-100 d-flex flex-column">
                            <div style="position: relative;">
                                <img src="@item.ImageUrl" class="card-img-top rounded-3 mb-3" alt="@item.Title" style="height: 250px; object-fit: cover; width: 100%;">
                                <span class="badge bg-warning text-dark position-absolute top-0 start-0 m-3 shadow-sm rounded-pill px-3 py-2">
                                    🏠 Kiralık Oda
                                </span>
                            </div>
                            <h5 class="fw-bold text-truncate mb-1">@item.Title</h5>
                            <p class="text-muted small mb-2 d-flex align-items-center gap-1">
                                <i class="bi bi-geo-alt-fill text-warning"></i> @item.Location
                            </p>
                            <div class="d-flex justify-content-between align-items-center mt-3 pt-3 border-top flex-grow-1 align-items-end">
                                <span class="fw-bold fs-5 text-dark">@(item.Price == 0 ? "Ücretsiz" : "₺" + item.Price.ToString("N0"))</span>
                                <a href="/Listings/Details/@item.Id" class="btn btn-sm btn-outline-dark rounded-pill px-3">İncele</a>
                            </div>
                        </div>
                    </div>
                }
            </div>
        }
        else
        {
            <div class="alert alert-light text-center border">
                Henüz kiralık oda ilanı bulunmuyor.
            </div>
        }
    </div>

    <hr class="my-5 text-muted opacity-25">

    <!-- Items Section -->
    <div class="fade-in-up" style="animation-delay: 0.2s;">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2 class="fw-bold"><i class="bi bi-box-seam text-warning me-2"></i>Satılık Eşya</h2>
            <a asp-action="Items" class="btn btn-outline-dark rounded-pill btn-sm px-3">Tümünü Gör</a>
        </div>

        @if (Model.Items != null && Model.Items.Any())
        {
            <div class="row g-4">
                @foreach (var item in Model.Items.Take(3)) // Show first 3 here
                {
                    <div class="col-md-4">
                        <div class="card-custom h-100 d-flex flex-column">
                            <div style="position: relative;">
                                <img src="@item.ImageUrl" class="card-img-top rounded-3 mb-3" alt="@item.Title" style="height: 250px; object-fit: cover; width: 100%;">
                                <span class="badge bg-secondary text-white position-absolute top-0 start-0 m-3 shadow-sm rounded-pill px-3 py-2">
                                    📦 Satılık Eşya
                                </span>
                            </div>
                            <h5 class="fw-bold text-truncate mb-1">@item.Title</h5>
                            <p class="text-muted small mb-2 d-flex align-items-center gap-1">
                                <i class="bi bi-geo-alt-fill text-warning"></i> @item.Location
                            </p>
                            <div class="d-flex justify-content-between align-items-center mt-3 pt-3 border-top flex-grow-1 align-items-end">
                                <span class="fw-bold fs-5 text-dark">@(item.Price == 0 ? "Ücretsiz" : "₺" + item.Price.ToString("N0"))</span>
                                <a href="/Listings/Details/@item.Id" class="btn btn-sm btn-outline-dark rounded-pill px-3">İncele</a>
                            </div>
                        </div>
                    </div>
                }
            </div>
        }
        else
        {
            <div class="alert alert-light text-center border">
                Henüz satılık eşya ilanı bulunmuyor.
            </div>
        }
    </div>
</div>
