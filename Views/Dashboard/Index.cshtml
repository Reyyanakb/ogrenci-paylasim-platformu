@model mvcFinal2.ViewModels.DashboardViewModel

@{
    ViewData["Title"] = "Kullanıcı Paneli";
}

<div class="container section-padding">
    <div class="row">
        <!-- Sidebar Menu -->
        <partial name="_DashboardSidebar" model="Model" />

        <!-- Main Content Area -->
        <div class="col-md-9">
            <!-- Welcome Info & Rating -->
            <div class="row mb-4">
                 <div class="col-md-8">
                    <div class="card-custom p-4 h-100 bg-primary-light" style="background-color: #FFFDE7; border-left: 5px solid var(--primary-color);">
                        <div class="d-flex justify-content-between align-items-center h-100">
                            <div>
                                <h4 class="fw-bold">Hoş Geldin, @(User.Identity?.Name ?? "Kullanıcı")! 👋</h4>
                                <p class="mb-0 text-muted">Bugün senin için yeni ilanlar olabilir, bildirimlerini kontrol etmeyi unutma.</p>
                            </div>
                        </div>
                    </div>
                 </div>
                 <div class="col-md-4">
                     <a href="/Users/Details/@Model.CurrentUserId#reviews-section" class="text-decoration-none text-dark h-100 d-block">
                         <div class="card-custom p-3 h-100 d-flex flex-column justify-content-center align-items-center text-center transition-hover">
                             <div class="text-warning mb-1">
                                 @if(Model.User?.AverageRating > 0) {
                                     <span class="fs-4 fw-bold text-dark me-1">@Model.User.AverageRating.ToString("0.0")</span>
                                     @for(int i=1; i<=5; i++) {
                                         if(i <= Math.Round(Model.User.AverageRating)) { <i class="bi bi-star-fill"></i> }
                                         else { <i class="bi bi-star text-muted opacity-25"></i> }
                                     }
                                 } else {
                                      <span class="text-muted small">Henüz Değerlendirme Yok</span>
                                 }
                             </div>
                             <small class="text-muted">(@(Model.User?.ReviewCount ?? 0) Değerlendirme)</small>
                             <h6 class="fw-bold mt-2 mb-0">Satıcı Puanı</h6>
                             <small class="text-primary mt-2">Detaylar için tıkla <i class="bi bi-arrow-right"></i></small>
                         </div>
                     </a>
                 </div>
            </div>

            <!-- Stats / Quick Info -->
            <div class="row mb-4">
                <div class="col-md-4">
                    <div class="card-custom p-3 text-center">
                        <h3 class="fw-bold mb-1">@Model.Listings.Count</h3>
                        <small class="text-muted">Aktif İlanım</small>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card-custom p-3 text-center">
                        <h3 class="fw-bold mb-1">@Model.FavoritesCount</h3>
                        <small class="text-muted">Favori</small>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card-custom p-3 text-center">
                        <h3 class="fw-bold mb-1 @(Model.UnreadMessageCount > 0 ? "text-danger" : "")">@Model.UnreadMessageCount</h3>
                        <small class="text-muted">Okunmamış Mesaj</small>
                    </div>
                </div>
            </div>

            <!-- Recent Listings Table -->
            <div class="card-custom p-4">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h5 class="fw-bold mb-0">İlanlarım</h5>
                    <a asp-action="CreateListing" class="btn btn-sm btn-primary-custom">+ Yeni İlan Ekle</a>
                </div>
                
                <div class="table-responsive">
                    <table class="table table-hover align-middle">
                        <thead class="table-light">
                            <tr>
                                <th>İlan Başlığı</th>
                                <th>Kategori</th>
                                <th>Fiyat</th>
                                <th>Durum</th>
                                <th>İşlem</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.Listings)
                            {
                                <tr>
                                    <td>
                                        <div class="d-flex align-items-center">
                                            <img src="@item.ImageUrl" class="rounded me-2" alt="" style="width: 40px; height: 40px; object-fit: cover; @(item.IsCompleted ? "filter: grayscale(1);" : "")">
                                            <span class="fw-bold small @(item.IsCompleted ? "text-muted text-decoration-line-through" : "")">@item.Title</span>
                                        </div>
                                    </td>
                                    <td>@(item.Type == "Room" ? "Oda" : "Eşya")</td>
                                    <td>@(item.Price == 0 ? "Ücretsiz" : "₺" + item.Price.ToString("N0"))</td>
                                    <td>
                                        @if (item.IsCompleted)
                                        {
                                            <span class="badge bg-secondary">@(item.CompletedType == mvcFinal2.Models.ListingCompletionType.Sold ? "Satıldı" : "Kiralandı")</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-success">Yayında</span>
                                        }
                                    </td>
                                    <td>

                                        <div class="d-flex gap-1">
                                            @if (item.IsCompleted)
                                            {
                                                <form asp-controller="Listings" asp-action="ReactivateListing" asp-route-id="@item.Id" method="post" style="display:inline;">
                                                    <button type="submit" class="btn btn-sm btn-warning border text-dark" title="Tekrar Yayına Al"><i class="bi bi-arrow-repeat"></i></button>
                                                </form>
                                            }
                                            else
                                            {
                                                <a href="/Dashboard/EditListing/@item.Id" class="btn btn-sm btn-light border text-primary" title="Düzenle"><i class="bi bi-pencil"></i></a>
                                            }
                                            <a href="/Listings/Details/@item.Id" class="btn btn-sm btn-light border" title="Görüntüle"><i class="bi bi-eye"></i></a>
                                            <form asp-action="DeleteListing" method="post" onsubmit="return confirm('Bu ilanı silmek istediğinize emin misiniz?');" style="display:inline;">
                                                <input type="hidden" name="id" value="@item.Id" />
                                                <button type="submit" class="btn btn-sm btn-light border text-danger" title="Sil"><i class="bi bi-trash"></i></button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                            }
                            @if (!Model.Listings.Any())
                            {
                                <tr>
                                    <td colspan="5" class="text-center text-muted py-4">Henüz bir ilanın yok.</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
                </div>
            </div>
            
            <!-- Recent Reviews Section -->
             @if (Model.User?.ReviewsReceived != null && Model.User.ReviewsReceived.Any())
             {
                 <div class="card-custom p-4 mt-4">
                     <h5 class="fw-bold mb-4">Son Değerlendirmeler</h5>
                     <div class="list-group list-group-flush">
                         @foreach (var review in Model.User.ReviewsReceived.OrderByDescending(r => r.CreatedAt).Take(3))
                         {
                             <div class="list-group-item border-0 px-0 py-3">
                                 <div class="d-flex justify-content-between mb-2">
                                     <div class="d-flex align-items-center gap-2">
                                          <div class="bg-light rounded-circle d-flex align-items-center justify-content-center" style="width: 32px; height: 32px;">
                                              <i class="bi bi-person-fill text-muted"></i>
                                          </div>
                                          <span class="fw-bold small">@(review.Reviewer?.FullName ?? "Anonim")</span>
                                          <small class="text-muted ms-2">@review.CreatedAt.ToString("dd MMM")</small>
                                     </div>
                                     <div class="text-warning small">
                                         @for (int i = 1; i <= 5; i++)
                                         {
                                             <i class="bi @(i <= review.Rating ? "bi-star-fill" : "bi-star")"></i>
                                         }
                                     </div>
                                 </div>
                                 <p class="mb-0 text-muted small bg-light p-3 rounded-3 fst-italic">"@review.Comment"</p>
                             </div>
                         }
                     </div>
                     @if(Model.User.ReviewCount > 3) {
                         <div class="text-center mt-3 pt-3 border-top">
                             <a href="/Users/Details/@Model.CurrentUserId" class="btn btn-sm btn-link text-decoration-none">Tümünü Profilimde Gör</a>
                         </div>
                     }
                 </div>
             }
        </div>
    </div>
</div>
